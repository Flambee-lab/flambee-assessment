import {absoluteUrl,indexable} from '../../lib/publication';
import type {Metadata} from 'next';
import {articles} from '../../content/articles-v3';
export const metadata:Metadata={title:'Guías de UX y automatización para empresas | Flambee',alternates:{canonical:absoluteUrl('/blog')},robots:{index:indexable,follow:true},description:'Respuestas prácticas sobre pedidos, presupuestos, atención, documentos y productos digitales. Explorá qué mejorar y cómo comprobarlo.'};
const categories=[...new Set(articles.map(a=>a.category))];
export default function Blog(){return <main id="contenido" className="blog-index"><div className="blog-intro"><span className="eyebrow">GUÍAS PARA DECIDIR MEJOR</span><h1>Encontrá qué vale la pena mejorar.</h1><p>Partí de una situación de tu empresa. Explorá alternativas y comprobaciones antes de decidir si necesitás diseño, automatización o IA.</p></div><section className="starting-points" aria-labelledby="starting-title"><h2 id="starting-title">Empezá por lo que te pasa</h2><div className="starting-grid">{[{title:'Repetimos tareas a mano',text:'Elegí qué conviene automatizar primero.',slug:'elegir-primera-automatizacion'},{title:'Los clientes necesitan ayuda',text:'Explorá si hace falta un chatbot o mejorar la experiencia.',slug:'chatbot-o-mejorar-ux'},{title:'La IA necesita muchas correcciones',text:'Detectá qué cambia entre una demo y los casos reales.',slug:'piloto-ia-casos-reales'}].map(item=><a className="starting-card" href={'/blog/'+item.slug} key={item.slug}><strong>{item.title}</strong><span>{item.text}</span><small>Ver por dónde empezar ↗</small></a>)}</div></section><div className="assessment-invite"><div><strong>¿Querés revisar cómo se aplica a tu empresa?</strong><p>El recorrido guiado te ayuda a encontrar una oportunidad para explorar y un próximo paso. No necesitás conocimientos técnicos.</p></div><a className="blog-button" href="/">Evaluar mi empresa ↗</a></div><h2 className="catalog-title">Todas las guías <span>({articles.length})</span></h2><nav className="category-nav" aria-label="Temas de las guías">{categories.map((c,i)=><a key={c} href={'#tema-'+i}>{c}</a>)}</nav>{categories.map((c,i)=><section id={'tema-'+i} key={c} className="topic-section"><div className="topic-heading"><span>0{i+1}</span><h2>{c}</h2></div><div className="article-grid">{articles.filter(a=>a.category===c).map(a=><article className="article-card" key={a.slug}><span className="eyebrow">GUÍA PRÁCTICA</span><h3><a href={'/blog/'+a.slug}>{a.title}</a></h3><p>{a.description}</p><a className="read-link" href={'/blog/'+a.slug}>Leer la guía <span aria-hidden="true">↗</span></a></article>)}</div></section>)}</main>}
